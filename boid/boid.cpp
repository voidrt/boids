using namespace std;
#include "boid.h"
#include "../squoid/squoid.h"
#include <raylib.h>
#include <raymath.h>
#include <unordered_map>
#include <string>
#include <vector>

void Boid::MoveBoid(const Boid flock[], int boidCount, const Squoid squoids[], int squoidCount, const unordered_map<string, vector<int>> &worldGrid)
{
    
}